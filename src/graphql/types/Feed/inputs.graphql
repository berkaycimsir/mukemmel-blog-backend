# addFeedInput for addFeed mutation
input addFeedInput {
  blog_id: String
  reply_id: String
  user_id: ID!
  content: String!
}

# updateFeedInput for feed update mutation
input updateFeedInput {
  id: ID!
  content: String!
}

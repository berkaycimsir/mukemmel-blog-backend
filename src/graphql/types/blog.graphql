scalar Date
scalar Int

type Query {
  blog(id: ID!): BlogResolverReturnType!
  getLastFourBlog: [Blog!]
  getTrendBlogs: [Blog!]
  blogs: [Blog!]
}

type Blog {
  id: ID!
  owner_id: ID!
  title: String!
  content: String!
  tags: [String]!
  likes: Int!
  views: Int!
  img: String!
  createdAt: Date!
  user: User
  comments: [Comment!]
}

type BlogResolverReturnType {
  blog: Blog
  errorMessage: String!
}

type Mutation {
  createBlog(data: createBlogInput!): BlogResolverReturnType!
}

input createBlogInput {
  owner_id: ID!
  title: String!
  content: String!
  tags: [String!]!
  likes: Int!
  views: Int!
  img: String!
}
